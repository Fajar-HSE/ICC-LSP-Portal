# Supabase (publik anon key — aman untuk client-side, RLS read-only)
# Salin ke .env untuk dev lokal: cp .env.example .env
VITE_SB_URL=https://ziybqtcdphuzhfoahopr.supabase.co
VITE_SB_KEY=YOUR_ANON_KEY_HERE

# Hanya untuk script server-side (JANGAN commit):
# SUPABASE_SERVICE_KEY=
# SUPABASE_DB_PASSWORD=
